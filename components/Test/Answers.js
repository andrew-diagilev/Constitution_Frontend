import React from 'react';
import {TouchableOpacity, View} from 'react-native';

export default function Answers({ options, handleAnswerSelection, isOptionsDisabled, currentOptionSelected, isQuestionAnswered }) {
    return (<View></View>)};